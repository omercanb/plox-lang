# Plox

This is a work in progress custom domain specific language for the D20 board game cafe app.
It aims to provide a method to do algorithmic trading on top of the trading system.

Testing this language is done with expect tests, (.expect) files, this means output is generated by the program and it's checked on following runs that the output stays the same. The tests for the language are currently held separate from the main program.
